[Unit]
Description=Nextcloud Data Dump
After=network.target

[Service]
Type=simple
User=root
Group=root
EnvironmentFile={{ cs_nc_root_dir }}/.create_dump.env
ExecStart={{ cs_nc_root_dir }}/create_dump.sh

[Install]
WantedBy=multi-user.target
